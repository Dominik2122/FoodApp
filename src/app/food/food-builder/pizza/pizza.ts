export interface Pizza {
  tomatoes: { amount: number, price: number };
  beans: { amount: number, price: number };
  corn: { amount: number, price: number };
  cheese: { amount: number, price: number };
  peppers: { amount: number, price: number };
  onions: { amount: number, price: number };
}
